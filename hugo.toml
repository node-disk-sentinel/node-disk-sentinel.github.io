baseURL = 'https://node-disk-sentinel.org/'
languageCode = 'en'
title = 'Node Disk Sentinel'

# Disable unused kinds for a pure landing page to avoid taxonomy/rss warnings
disableKinds = ['taxonomy', 'term', 'RSS']

[params]
  description = 'Predict and handle failing Kubernetes node disks before workloads are impacted.'
  tagline = 'Disk failure is inevitable. Downtime isn’t.'
  github_repo = 'https://github.com/node-disk-sentinel/node-disk-sentinel'
  license = 'Apache-2.0'
  image = 'ghcr.io/node-disk-sentinel/node-disk-sentinel'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
